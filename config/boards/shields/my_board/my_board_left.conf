# Split
CONFIG_ZMK_SPLIT=y
CONFIG_ZMK_SPLIT_BLE=y

# Peripheral 쪽에서는 ROLE_CENTRAL을 "설정하지 않는다" (n으로 박지 말 것)
# USB도 Peripheral에서는 사용 안 함
CONFIG_ZMK_USB=n

# (선택) 디버그 로그 필요할 때만
# CONFIG_LOG=y
# CONFIG_ZMK_LOG_LEVEL_INF=y
